# 自触发模型仿真复现性现状

## 1. 背景

自触发模型需要准确的评估，以确定优化效果和上线准入。目前方法是通过仿真进行计算，折算为准召对比看板指标。

即：模型每次迭代要回答两件事——**优化效果**（新模型相比旧模型准召是升是降）和**上线准入**（能否达到看板门槛、可以发布）。由于线上真实准召只能在发版后统计，发版前需要用**离线仿真**预估，再折算进与线上看板同一套准召口径来做版本对比与准入判断。这条链路成立的前提是：**仿真能忠实复现路测当时的触发行为**。

---

## 2. 场景数据

### 2.1 数据来源

写了一批离线脚本，可以在数梦平台定时（每周一）挖掘路测每个版本 RA 的 issue，或正常等待的场景，保存到场景集中。

- 离线脚本：https://kunpeng.xiaojukeji.com/view/revision/5803747
- 数梦定时任务：
  - issue 转 scenario：link
  - 正常等待：link

挖掘逻辑（对应 `issue_to_scenairo.py`）：按版本 / `ra_type` / `ra_trigger` 等条件筛 RA issue，以首个 RA `start` 事件为锚取 tripSegment 时间窗（`[start-20s, start+5s]`，warmup 3s），逐 issue 建 scenario 并打标签落入场景集。「正常等待」类作为负样本 / 无需触发场景补充进来。

### 2.2 场景分布

Q2 模型上线前总结了一个若干个版本的数据，包含：

- release 0206 两个版本，包含 review 和未 review 版本
- release 0327 - release 0417 四个版本
- release 0508 - release 0522 三个版本

上述版本均可以通过场景标签进行查询。如 0327 版本的正确触发 / 无需协助场景，可以通过「**版本标签 + 触发标签（correct_trigger / 无需协助）**」的标签组合查询：在 Scenario Management 中输入指定标签组合查询，即可以得到对应场景。

> 标签由挖掘脚本写入（`scenario_label`），典型含：版本、`scenario_from_issue`、`data_sim`、触发类别（`correct_trigger` / `mis_trigger`）、`ra_trigger`、日期等。按「版本 + 触发类别」组合即可切出该版本的正 / 负样本集。

---

## 3. 仿真 & 评估

### 3.1 仿真复现性链路

自触发模型上线前，需要对模型上线后的准召进行预估，因此首先需要保证离线仿真结果计算出来的准召和线上一致，也即保证以下两项指标的一致性：

1. **线上路测数据 & 离线挖掘一致性**：即通过离线挖掘的 TP/FP/FN 计算得到的准召和线上路测准召一致；
2. **离线挖掘数据 & 仿真一致性**：在离线挖掘的数据上，用对应版本的 binary 进行开环仿真，能够接近 100% 复现自触发场景，即数据标签为正（正确触发 / 无需协助）的场景在仿真中也触发，标签为负（误触发）的场景在仿真中也不触发。

其中：

- 第一项的一致性通常是由场景挖掘来决定的（即第 2 节场景数据），一般情况计算得到的准召和线上差距不大；
- 第二项则需要保证仿真能够几乎 100% 复现离线挖掘场景的触发标签。

**当前的瓶颈在第二项**：仿真对路测触发存在系统性欠触发，导致离线挖掘数据与仿真不一致（见 3.3）。

### 3.2 仿真准召预估方式

在离线挖掘并打好标签的场景集上，用**对应版本的 binary** 做开环 PLANNING 仿真，读仿真是否发出 RA 请求，再按标签构造混淆矩阵、折算准召。

**触发判定（读仿真产物）**：以 `dpe_assist_channel_triggered`（是否真正发出 RA 请求，最贴看板口径）为主，`dpe_stuck_detect`（模型是否检测到卡顿）为辅。批量结果从 Trail Orion job 拉取（`ra_api/sim_result_api.py`、`check_sim/repro/get_scenario_ids.py`），支持 Base vs Feature 两个 job 对比。

**混淆矩阵（标签 × 仿真是否触发）**：

| 场景标签 | 仿真触发 | 仿真未触发 |
|---|---|---|
| 正（正确触发 / 无需协助） | TP | FN（复现失败 / 漏检） |
| 负（误触发） | FP | TN（正确抑制） |

**准召折算（与线上看板同口径）**：

- 仿真准确率 = TP / (TP + FP)
- 仿真召回率 = TP / (TP + FN)
- 正样本复现率 = TP / (TP + FN)（= 召回率，衡量该触发的仿真触没触）
- 负样本复现率 = TN / (TN + FP)（衡量该抑制的仿真抑没抑）

**版本对比**：对每个 release 版本用其自身 binary 跑同版本场景集，得到该版本的仿真准召；新旧版本在**同一场景集**上对比，判断优化效果与是否满足准入。

> 复现必须锁死的工具性要素（否则准召被污染，属工具偏差非模型问题）：① 用路测同版本 binary（如 1665523），DefaultBuild 行为已变、不可比；② 地图 `skip_map_update=False`（用 trip 图，等价 `--use-trip-hdmap`），EzSim server 当前图会造成车道错配伪不触发；③ 指定 binary 时 `skip_model_update=False`，否则缺模型文件启动 abort；④ 开 `--planning_enable_warmup_frame_state/seed` 恢复跨帧状态（实测生效）；⑤ `--sim_smart_agent` 会用 DL agent 驱动前车，实测不挪位置但可能丢逐帧感知属性。

### 3.3 目前仿真复现性存在的问题

#### 3.3.1 现状

**仿真对路测触发存在系统性「欠触发」（路测触发、仿真不触发），召回被显著低估；准确率侧受影响较小。** 表现为：标签为正的场景在仿真中大量未触发（FN 偏高），离线挖掘数据与仿真不一致，第二项一致性不达标，导致仿真折算出的召回低于线上真实召回。

实测证据：

- **cn32318631**（trip 10385_20260525）：路测触发 RA 的 **10 个 scenario，EzSim 仿真全部未触发**（`is_opened=False`）。
- **cn32422765**（trip 10498_20260527）：路测 `assist_request=1`（触发），cloud_sim 无该 topic（未触发），甚至多给 13s warmup 仍不触发。

即：正样本复现率明显偏低，仿真召回 < 线上召回，当前尚不足以直接作为上线准入的**召回门槛**依据。

#### 3.3.2 原因初步分析

未复现（欠触发）的场景可归为三大根因：

| 类 | 名称 | 表现 | 机理 |
|---|---|---|---|
| **A** | FP 规则压制 | sim 出现 `AssistStuckRequestForbidByLaneChange`；`unstuck_status=MODEL_FP`；常伴 `RouteUnstuck(kRelaxLaneMarking)` | 模型即使触发也被换道 FP abort → 欠触发 |
| **B** | 场景发散 | sim 出现 `RouteUnstuck` / `receive_fix_point_from_cloud`，ego 自行脱困，模型分数 / 计数器不积累 | 仿真走了路测没走的路 → 无从触发 |
| **C** | 模型分数不足 / 计数器被清零 | scen_dnn 低于阈值或 =-1（模型未运行）；FN forcing recall `accumulated_cycle` 到不了阈值 | 欠触发的主力类型 |

**C 类的深度定论（2026-06 逐帧实测收敛，已纠正早期误判）**：

- ❌ **不是「状态不持久化」**：静止时长 `observed_to_static_ts`、forcing 计数 seed 在 warmup 下均能正确恢复，dump/load 机制 work。
- ✅ **真实根因 = `is_queuing` 在仿真里过度触发**，把 forcing-recall 计数器反复清零（190/331 帧误判为排队），计数器峰值到过 70 却到不了 trigger_cycle=200。
- 逐行链路：前车语义标签 `ReasonStationary` 路测判 `kTempParked`、cloud_sim 掉成 `kYielding` → ego `kTempParked/kQueuing` 翻转 → `is_queuing` false/true → 计数器累积触发 / 清零饿死。
- **头号嫌疑**：前车**右转灯灯态**（`has_right_turn_signal_on`，`ReasonStationary` path A）**在 cloud_sim 里丢失**——smart_agent 复现了位置却不产出转向灯灯态，导致 path A 失效、掉到默认 `kYielding`。属**感知保真问题，非模型能力问题**（congestion 关联证据支撑；转向灯不在 dump 范围，为最强嫌疑而非已钉死）。

**已逐一排除的假设（勿再重查）**：状态不持久化 / `is_long_term_static` 闸门未带入（`observed_to_static_ts` 实测一致）、车道序列/路由/地图发散（EzSim 用错地图的 artifact）、smart_agent 挪障碍物位置（逐字段一致）、`routing_enable_warmup_frame_state`（加上仍发散）。

**对准召的影响**：C 类根因是感知/语义属性在回放中丢失，会把本该触发的正样本压成 FN → 系统性低估召回；准确率侧（FP）相对稳定。

---

## 4. 指标看板

监控不同版本的仿真复现性指标，量化「离线挖掘数据 & 仿真一致性」（3.1 第二项）随版本的变化，作为准入与优化评估的支撑。

**建议监控的指标（按 release 版本分行：0206 review/未 review、0327–0417、0508–0522 …）**：

| 指标 | 定义 | 目标 |
|---|---|---|
| 正样本复现率 | TP / (TP + FN)，正样本仿真触发比例 | → 100% |
| 负样本复现率 | TN / (TN + FP)，负样本仿真抑制比例 | → 100% |
| 仿真准确率 | TP / (TP + FP) | 对齐线上 |
| 仿真召回率 | TP / (TP + FN) | 对齐线上 |
| 准召 gap | (仿真准召 − 线上看板准召) | → 0 |
| 未复现场景根因分布 | A / B / C 各占比 | 定位收敛方向 |

**数据来源**：cloud_sim（Orion job）批量跑各版本场景集 → `dpe_assist_channel_triggered` / `dpe_stuck_detect` 折算（`sim_result_api.py`）；线上看板准召来自 `q2_model_master_stats.py`（版本→RA触发源计数 + 逐日准召）。两者对齐即可读出 gap。

**当前读数（定性）**：正样本复现率偏低（欠触发主导），召回 gap 明显为负；准确率 gap 较小。根因分布以 C 类（感知保真 / 计数器清零）为主，A 类（FP 压制）次之。

---

## 附：关键工具与源码索引

| 用途 | 位置 |
|---|---|
| 触发 EzSim 仿真 | `check_sim/repro/ezsim.py` |
| issue → scenario 挖掘 | `issue_to_scenairo.py`（线上定时脚本见 2.1 Kunpeng 链接） |
| scenario 查询 / 标签 | `ra_api/scenario_api.py` |
| 仿真结果 DPE 查询 | `ra_api/sim_result_api.py`、`check_sim/repro/get_scenario_ids.py` |
| 看板准召统计 | `scripts/q2_model_master_stats.py` |
| 读 bag 信号 | `check_sim/bag/read_bag.py`、skill `ezsim-workflow.md` / `ra-bag-topics.md` |
| 触发全链路逻辑 | skill `trigger-logic.md` |
| 已知发散 / 修复（实时更新） | skill `sim-repro-fixes.md` |
