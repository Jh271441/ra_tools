data/
!ra_sim_repro_dashboard/data/
!ra_sim_repro_dashboard/data/mock/
.idea/
.venv/
**/*.xlsx
**/*.pyc
*.csv
*.png
*.json
!ra_triage_dashboard/config/model_profiles.json
!ra_sim_repro_dashboard/frontend/package.json
!ra_sim_repro_dashboard/frontend/package-lock.json
!ra_sim_repro_dashboard/frontend/tsconfig.json
!ra_sim_repro_dashboard/data/mock/*.json
model_release_pipeline/.runs/
model_release_pipeline/web_frontend/node_modules/
model_release_pipeline/web_frontend/dist/
model_release_pipeline/web_frontend/.vite/

# Local proxy/account state.
cc-connect.service
ops/cpa/auth-dir/
ops/cpa/config.yaml
ops/gateway/.env

# Ares Playwright credentials, browser profile, and local screenshots.
ares_storage_state.json
ares_chrome_profile/
playwright_screenshots/

# Onnx model files
*.onnx*

# Local scratch: one-off probes, agent temp dirs, tree copies (keep outside Git)
.codex_tmp*/
.tmp_*
prepare_release*_probe*.py
ra_triage_dashboard.backup-*/
scripts/inspect_*.py
auto_triage_bot/.data/

# macOS Finder junk
.DS_Store

# Local extras that are not ra_tools product source.
/knowledge_backup/
.claude/
/ra_triage_dashboard/docs/sso-incident-20260913.md

# Generated results and audit tree snapshots (keep outside Git)
outputs/

# Build/release artifacts
*.zip

# Local-only RA experiment payloads. Versioned source and receipts live in the
# cloud experiment branches; raw datasets, predictions, and caches remain here
# until their destination receives explicit data-transfer approval.
local_experiment_artifacts/

# Safe deployment examples (runtime credentials/config remain ignored).
!/ops/sim_deploy/target.example.json
!/ops/sim_deploy/weekly.example.json
